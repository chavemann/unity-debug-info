docfx docfx.json --serve
REM After the above command, open a new console and run `docfx docfx.json` to update.
REM Depending on changes it may be necessary to clear out the `_sites` and `api/*.yml` files,
REM and run `docfx metadata` to regenerate some of the api yml files.
